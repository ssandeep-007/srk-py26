Calculate length of list using Nested function
---------------------------------------------------------

WAP to read elements from user to store into a list.  
1. The list should be passed to a function.  
2. The function has to use a nested member function to find the length of the list.  
3. The outer function should return the length of the list to the calling function  


#CODE
def length(list1):
    def findLen():
        n=len(list1)
        return n
    return findLen()
l1=[12,98,76,32,10]
res=length(l1)
print('length is: ',res)
